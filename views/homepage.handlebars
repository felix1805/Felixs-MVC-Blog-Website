{{#each posts as |blogpost|}}
<div class="row mb-4 project">
  <div class="col-md-5">
    <p>
      Posted by {{user.name}} on {{format_date date_created}}
    </p>
  </div>
  <div class="col-md-7">
    <p>
      {{blogpost.title}}
      <br></br>
{{blogpost.content}}
    </p>
  </div>
</div>

{{/each}}